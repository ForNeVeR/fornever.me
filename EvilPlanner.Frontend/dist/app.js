var React = require('react');
var ReactDOM = require('react-dom');
var QuotationBlock = require('./quotation-block');
ReactDOM.render(React.createElement(QuotationBlock, null), document.getElementById('content'));
